#!/bin/bash

#if ! command -v mvn -version &> /dev/null
#then
#    sudo apt-get install maven
#fi
#
#mvn clean install

#docker build . --tag=telegram/nis:v1
#docker buildx build --platform linux/amd64 . --tag=camunda/core:v1

docker compose up -d